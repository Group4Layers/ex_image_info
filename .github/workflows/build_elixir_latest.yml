name: "Build 1.18/26"

on:
  push:
    branches:
      - 'master'
  pull_request:
    branches:
      - 'master'

jobs:
  elixir_otp_latest:
    uses: ./.github/workflows/build_reusable.yml
    with:
      elixir-version: 1.18
      otp-version: 26
